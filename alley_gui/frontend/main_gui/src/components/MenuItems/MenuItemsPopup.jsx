import React from 'react';

const MenuItemsPopUp = ({ open, onClose, data, onCheckboxChange }) => {
  /* The code for your Dialog component with checkbox rendering */
};

export default MenuItemsPopUp;
